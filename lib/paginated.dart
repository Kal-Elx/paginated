import 'package:flutter/material.dart';

// TODO: Implement
class Paginated extends StatelessWidget {
  const Paginated({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
